let hobbies = ["Volleyball", "Watching movies", "Reading"]
hobbies.map(function(h) {
    console.log(h)
})

let student = { name: "Patrick", age: 21 }
let name = student.name
let age = student.age
console.log(name, age)

let numbers = [1, 2, 3]
let moreNumbers = [...numbers, 4, 5]
console.log(moreNumbers)
